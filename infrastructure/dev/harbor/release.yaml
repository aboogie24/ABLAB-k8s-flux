apiVersion: helm.toolkit.fluxcd.io/v2beta2 
kind: HelmRelease
metadata:
  name: harbor
  namespace: harbor
spec: 
  chart: 
    spec:
      chart: harbor
      version: 24.0.0
      interval: 5h 
      sourceRef:
        kind: HelmRepository
        name: harbor
  interval: 1h 
  releaseName: harbor
  