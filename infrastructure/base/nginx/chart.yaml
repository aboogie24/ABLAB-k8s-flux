# Grab the nginx ingress controller chart

apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: helmChart
metadata: 
  name: nginx
  namespace: nginx 
spec: 
  chart: nginx-ingress
  version: 1.4.x
  interval: 5h 
  sourceRef:
    kind: HelmRepository
    name: nginx
